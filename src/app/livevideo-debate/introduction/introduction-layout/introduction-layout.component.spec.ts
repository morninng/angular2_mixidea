/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IntroductionLayoutComponent } from './introduction-layout.component';

describe('Component: IntroductionLayout', () => {
  it('should create an instance', () => {
    let component = new IntroductionLayoutComponent();
    expect(component).toBeTruthy();
  });
});
