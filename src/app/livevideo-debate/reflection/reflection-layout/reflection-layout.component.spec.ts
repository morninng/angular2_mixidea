/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ReflectionLayoutComponent } from './reflection-layout.component';

describe('Component: ReflectionLayout', () => {
  it('should create an instance', () => {
    let component = new ReflectionLayoutComponent();
    expect(component).toBeTruthy();
  });
});
