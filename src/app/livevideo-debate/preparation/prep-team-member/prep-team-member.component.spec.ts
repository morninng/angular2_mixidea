/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PrepTeamMemberComponent } from './prep-team-member.component';

describe('Component: PrepTeamMember', () => {
  it('should create an instance', () => {
    let component = new PrepTeamMemberComponent();
    expect(component).toBeTruthy();
  });
});
