/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PreparationLayoutComponent } from './preparation-layout.component';

describe('Component: PreparationLayout', () => {
  it('should create an instance', () => {
    let component = new PreparationLayoutComponent();
    expect(component).toBeTruthy();
  });
});
