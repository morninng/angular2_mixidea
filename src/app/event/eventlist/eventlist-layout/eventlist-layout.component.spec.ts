/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EventlistLayoutComponent } from './eventlist-layout.component';

describe('Component: EventlistLayout', () => {
  it('should create an instance', () => {
    let component = new EventlistLayoutComponent();
    expect(component).toBeTruthy();
  });
});
